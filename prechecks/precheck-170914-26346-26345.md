# Pre-check 分析报告：3 个赚钱候选综合评估

**分析日期**: 2026-05-17 01:08  
**分析 Agent**: Hermes Agent (subagent)  
**数据来源**: opportunity-radar candidates_2026-05-17.json

---

## 候选总览

| # | 候选 | 赏金 | 预估时薪 | 分数 |
|---|------|------|---------|------|
| 1 | home-assistant/core#170914 — Commit `.devcontainer/devcontainer-lock.json` | $100 | $22.56/hr | 56.4 |
| 2 | strapi/strapi#26346 — better-sqlite3 依赖位置导致 Docker ARM64 构建损坏 | $75 | $16.63/hr | 56.14 |
| 3 | strapi/strapi#26345 — TS 项目运行强制要求源码存在 | $75 | $16.63/hr | 56.14 |

---

## 候选 1: home-assistant/core#170914

**URL**: https://github.com/home-assistant/core/issues/170914  
**标题**: Commit `.devcontainer/devcontainer-lock.json` to the repository  
**作者**: sbeitzel  
**创建时间**: 2026-05-16T15:38:42Z（~9 小时前）  
**状态**: ✅ OPEN — 无 Assignee — 无 PR — 无评论 — 无标签

### Issue 健康度检查

| 检查项 | 状态 |
|-------|------|
| **Assignee** | ✅ 无（可自由认领） |
| **关联 PR** | ✅ 无（无竞争） |
| **标签** | ⚠️ 没有任何标签（可能导致维护者未注意到） |
| **问题描述清晰度** | 🟢 非常清晰——描述 devcontainer-lock.json 的作用、为什么需要提交、怎么做 |
| **可复现步骤** | 🟢 隐含（只需构建 devcontainer 一次即可生成 lockfile） |
| **维护者回应** | ⚠️ 无任何回应（issue 仅 9 小时新） |
| **社区反应** | 🟢 无讨论，无竞争 |

### 技术复杂度评估

**难度**: 🔵 **简单**（DevOps 任务，零代码修改）

**修复方案**: 
1. Clone 仓库
2. 在 devcontainer 中打开（或手动运行 `devcontainer build` 等命令）
3. 生成的 `devcontainer-lock.json` 会自动出现在 `.devcontainer/` 目录
4. `git add .devcontainer/devcontainer-lock.json && git commit`
5. 创建 PR

**预估代码/文件变更**: 
- 1 个文件（新文件）: `.devcontainer/devcontainer-lock.json`（自动生成，~50-200 行 JSON）
- 0 行手写代码

**预估耗时**: 
- 环境准备 + 构建 devcontainer: ~20-40 分钟
- PR 提交流程: ~10 分钟
- **总计: ~30-50 分钟**

**关键风险**: 
- 构建 devcontainer 需要 Docker 运行环境 ✓（本地有）
- Home Assistant devcontainer 构建较大，可能需要 ~10-15 分钟下载镜像
- 没有代码变更，review 应该很轻量

### 赏金评估

| 项目 | 值 |
|------|-----|
| 赏金值 | **$100** |
| 确认/推断 | **⚠️ 推断值** — Issue 本身未提及任何赏金/sponsor |
| 来源 | system: home-assistant/core known-bounty-repo 的 `expected_bounty_range: [100, 2000]` 下限值 |
| 支付类型 | sponsor（推断） |
| 支付确定性 | 中低 — HA 是有 bounty 历史的 proven-payer，但这个具体 issue 没有显式 sponsor 标签 |

---

## 候选 2: strapi/strapi#26346

**URL**: https://github.com/strapi/strapi/issues/26346  
**标题**: create-strapi-app places better-sqlite3 in 'dependencies' by default, breaking production multi-arch Docker builds on ARM64 under QEMU emulation  
**作者**: david-koller-xmv  
**创建时间**: 2026-05-15T16:28:23Z（~33 小时前）  
**状态**: ✅ OPEN — 无 Assignee — 无 PR

### Issue 健康度检查

| 检查项 | 状态 |
|-------|------|
| **Assignee** | ✅ 无（可自由认领） |
| **关联 PR** | ✅ 无（无竞争） |
| **标签** | 🟢 `issue: bug` + `severity: high` + `source: tooling` + `version: 5` — 质量高 |
| **问题描述清晰度** | 🟢 极其清晰——完整复现步骤、Dockerfile、错误日志、workaround |
| **可复现步骤** | 🟢 完整且可操作（Docker buildx multi-arch 命令） |
| **维护者回应** | ⚠️ 无官方维护者回应 |
| **社区反应** | ✅ 1 条评论（jshaofa-ui 已提供完整 fix 分析 + 代码 diff） |

### 技术复杂度评估

**难度**: 🔵 **简单**（~3 行条件逻辑修改）

**修复方案**（来自评论 jshaofa-ui）:
```javascript
// In create-strapi-app template logic:
if (selectedDatabase === 'sqlite') {
  dependencies['better-sqlite3'] = '^9.4.3';
} else {
  devDependencies['better-sqlite3'] = '^9.4.3';
}
```

**需要修改的文件**（预估）:
1. `packages/create-strapi-app/` 中的项目模板生成逻辑 — 在根据数据库选择生成 `package.json` 时做条件判断
2. **预估**: ~3-10 行代码修改，1-2 个文件

**预估耗时**: 
- 理解代码库结构（找到模板生成位置）: ~20-30 分钟
- 实现修改 + 测试: ~20 分钟
- PR 流程: ~15 分钟
- **总计: ~1 小时**

**关键风险**:
- 需要找到 `create-strapi-app` 中生成 `dependencies` 的具体代码位置
- 可能需要理解 Strapi 的脚手架流程
- 但评论已经给出了清晰的 fix 方向

### 赏金评估

| 项目 | 值 |
|------|-----|
| 赏金值 | **$75** |
| 确认/推断 | **⚠️ 推断值** — Issue 无显式赏金/sponsor 标记 |
| 来源 | system: strapi/strapi known-bounty-repo 推断（低于 range [100, 1000] 下限） |
| 支付类型 | sponsor（推断） |
| 支付确定性 | 中 — Strapi 是有 sponsor 记录的 repo，但无明确付款承诺 |

---

## 候选 3: strapi/strapi#26345

**URL**: https://github.com/strapi/strapi/issues/26345  
**标题**: strapi start (TS projects) requires tsconfig.json + src/ + config/ to exist in the production runtime, forcing source files into the final image  
**作者**: david-koller-xmv  
**创建时间**: 2026-05-15T16:27:56Z（~33 小时前）  
**状态**: ✅ OPEN — 无 Assignee — 无 PR

### Issue 健康度检查

| 检查项 | 状态 |
|-------|------|
| **Assignee** | ✅ 无（可自由认领） |
| **关联 PR** | ❌ 无直接 PR，但 ⚠️ **存在 3 个相关 PR** (#26266, #26270, #26265) 在相同代码区域（ts-utils boot path） |
| **标签** | 🟢 `issue: bug` + `severity: high` + `version: 5` + `source: core:core` |
| **问题描述清晰度** | 🟢 极其清晰——完整 Dockerfile 复现、错误分析、workaround |
| **可复现步骤** | 🟢 完整可操作 |
| **维护者回应** | ⚠️ 无官方回应 |
| **社区反应** | ✅ 1 条评论（jshaofa-ui 提供完整 fix 分析 + 代码 diff） |

### 技术复杂度评估

**难度**: 🟡 **中等**（~5 行代码 + 需要理解 boot 流程 + 存在重叠 PR 可能冲突）

**修复方案**（来自评论 jshaofa-ui）:
```typescript
// In ts-utils.ts, isUsingTypeScript():
export function isUsingTypeScript(appDir: string): boolean {
  // In production mode, detect TypeScript from compiled output
  if (process.env.NODE_ENV === 'production') {
    const distConfig = path.join(appDir, 'dist', 'config');
    if (fs.existsSync(distConfig)) {
      return true;
    }
  }
  // Existing logic for dev mode...
}
```

**需要修改的文件**（预估）:
1. `packages/core/core/src/app-builder/ts-utils.ts` — 修改 `isUsingTypeScript()`
2. **预估**: ~5-10 行代码修改，1 个文件

**预估耗时**: 
- 理解现有 `isUsingTypeScript()` 逻辑: ~15 分钟
- 实现 + 测试: ~20 分钟
- PR 流程: ~15 分钟
- **总计: ~50 分钟**

**关键风险**:
- ⚠️ **高竞争风险** — 存在 3 个开放 PR (#26266, #26270, #26265) 在修改同一个 `@strapi/typescript-utils` 模块
- 这些 PR 的目标是 lazy-load/eliminate tsUtils from boot path，如果合并后可能部分解决 #26345
- 提交 PR 可能遭遇 merge conflict 或被视为重复工作
- 需要跟踪这些 PR 的进展再决定是否动手

### 赏金评估

| 项目 | 值 |
|------|-----|
| 赏金值 | **$75** |
| 确认/推断 | **⚠️ 推断值** — Issue 无显式赏金/sponsor 标记 |
| 来源 | system: strapi/strapi known-bounty-repo 推断（低于 range [100, 1000] 下限） |
| 支付类型 | sponsor（推断） |
| 支付确定性 | 中 — 同上 |

---

## 对比分析

| 维度 | #170914 (devcontainer-lock) | #26346 (better-sqlite3) | #26345 (TS source req) |
|------|---------------------------|------------------------|----------------------|
| **难度** | ★☆☆☆☆ 最简单 | ★★☆☆☆ 简单 | ★★★☆☆ 中等 |
| **代码行数** | 0 （纯 CLI/DevOps） | ~3-10 行 | ~5-10 行 |
| **预估时间** | ~30-50 min | ~1 hr | ~50 min |
| **实际时薪** | **$120-200/hr** | **$75/hr** | **$90/hr** |
| **竞争风险** | ✅ 极低（无 PR，新 issue） | ✅ 低（无 PR） | ❌ 高（3 个重叠 PR） |
| **确认付款** | ⚠️ 推断值 | ⚠️ 推断值 | ⚠️ 推断值 |
| **维护者活跃** | 🟡 HA 大型项目 | 🟢 Strapi active dev | 🟢 Strapi active dev |
| **AI 友好度** | 🟢 无需写代码 | 🟢 清晰的条件逻辑 | 🟡 需要理解 boot 流程 |
| **PR 合并概率** | 🟢 高（无脑操作） | 🟢 高（bug 明确，fix 清晰） | 🟡 中等（重叠 PR 风险） |

### 实际时薪重算

系统给的是 $22.56/hr (#170914) 和 $16.63/hr (#26346/#26345)，但这些是基于过高的时间估算（~4.4-4.5 小时）。

按更现实的估算重新计算：

| 候选 | 赏金 | 预估实际时间 | **实际时薪** |
|------|------|-------------|------------|
| #170914 | $100 | ~0.5-0.8 hr | **$125-200/hr** |
| #26346 | $75 | ~1 hr | **$75/hr** |
| #26345 | $75 | ~0.8 hr | **~$94/hr** |

---

## 结论与推荐

### 最佳候选: 🥇 home-assistant/core#170914

**核心理由**:
1. **零代码修改** — 只需 build devcontainer → commit 生成的 lockfile，AI 可完全自动化
2. **无竞争** — 新 issue（9h），无 assignee，无 PR，无评论
3. **最高实际时薪** — $100 赏金 / ~30 分钟 = **~$200/hr**
4. **最低风险** — DevOps 操作，HA 是 proven-payer，即使赏金是推断值也有很好的曝光价值
5. **高合并概率** — 简单、无争议、对项目有明确收益（devcontainer 可复现性）

### 次选: 🥈 strapi/strapi#26346

- Fix 清晰（已预分析），竞争低，但 $75 赏金明显低于 HA 的 $100
- 适合作为 HA #170914 之后的第二选择

### 不推荐: ❌ strapi/strapi#26345

- 虽然 fix 也清晰，但存在 3 个在相同代码区域的重叠 PR，可能很快被部分解决
- 提交 PR 有高几率遭遇冲突或被视为重复，白费功夫

### 行动建议

1. **立即开始** `home-assistant/core#170914` — clone HA 仓库 → 构建 devcontainer → git add lockfile → PR
2. **完成后** 评估 `strapi/strapi#26346` 作为备选
3. **跳过** `strapi/strapi#26345` 直到那 3 个 PR 的状态明朗

---

*报告结束。所有 3 个候选的赏金均为系统推断值（非确认值），但 HA 和 Strapi 均为有 bounty 历史的 proven-payer repos。*
